# 쇼핑몰 주문 시스템 도메인

## 서비스 개요

쇼핑몰은 회원이 상품을 주문하는 온라인 커머스 서비스다.
쇼핑몰의 핵심 가치는 고객이 원하는 상품을 편리하고 안전하게 구매하는 경험을 제공하는 것이다.

유사한 서비스로 쿠팡, 네이버 쇼핑이 있다.

# 도메인 모델

---

## [ 상품 애그리거트 ]

### 상품 (Product)
_Aggregate Root_

#### 행위 & 규칙
- 상품 생성 : `static create(ProductCreateCommand command, IdGenerator idGenerator)`
  - 재고가 1 이상이면 `ON_SALE`, 0이면 `SOLD_OUT` 상태로 생성
  - 생성 시 식별자(`productId`) 할당
  - 이미지가 있는 경우 대표 이미지는 반드시 하나여야 한다
  - 대표 이미지는 `thumbnailImage`에, 나머지는 `images`에 저장
- 재고 추가 : `addStock(quantity)`
  - `DISCONTINUED` 상태인 상품은 재고를 추가할 수 없다
  - 재고 추가 후 수량이 1 이상이 되면 자동으로 `ON_SALE` 전환
- 재고 차감 : `deductStock(quantity)`
  - `DISCONTINUED` 상태인 상품은 재고를 차감할 수 없다
  - 보유 재고보다 많은 수량을 차감할 수 없다
  - 재고 차감 후 수량이 0이 되면 자동으로 `SOLD_OUT` 전환
- 상품 정보 수정 : `modify(ModifyProductCommand command)`
  - `DISCONTINUED` 상태인 상품은 수정할 수 없다
- 판매 중단 : `discontinue()`
  - 이미 `DISCONTINUED` 상태인 상품은 중단할 수 없다
  - `DISCONTINUED` 는 불가역 상태로 복구 불가
- 이미지 추가 : `addImage(AddProductImageCommand command, IdGenerator idGenerator)`
  - `DISCONTINUED` 상태인 상품은 이미지를 추가할 수 없다
  - 이미지는 최대 10개까지 등록 가능
- 이미지 삭제 : `removeImage(ProductImageId imageId)`
  - `DISCONTINUED` 상태인 상품은 이미지를 삭제할 수 없다
  - 존재하지 않는 이미지 ID로 삭제 시 예외 발생

#### 속성
- `id` : ProductId
- `name` : ProductName (VO)
- `description` : ProductDescription (VO)
- `price` : Money (VO)
- `stockQuantity` : StockQuantity (VO)
- `status` : ProductStatus
- `category` : ProductCategory
- `productImages` : ProductImages (VO)

### 상품 상태 (ProductStatus)
_Enum_
- `ON_SALE` : 판매 중
- `SOLD_OUT` : 품절 (재고 = 0)
- `DISCONTINUED` : 판매 중단 (불가역)

### 상품 카테고리 (ProductCategory)
_Enum_
- `FOOD` : 식품
- `ELECTRONICS` : 전자기기
- `CLOTHING` : 의류
- `BOOKS` : 도서
- `ETC` : 기타

### 상품 이미지 묶음 (ProductImages)
_Value Object_

#### 속성 & 규칙
- `productImages` : ProductImages (VO)
- 그외 이미지가 있는 경우 대표 이미지는 반드시 있어야 한다
- 대표 이미지 외 이미지는 최대 10개까지 등록 가능

### 상품 이미지 (ProductImage)
_Entity_

#### 속성
- `id` : ProductImageId
- `imageUrl` : ImageUrl (VO)

### 상품명 (ProductName)
_Value Object_

#### 속성 & 규칙
- `value` : String
  - null, 공백 불가
  - 1자 이상 100자 이하

### 상품 설명 (ProductDescription)
_Value Object_

#### 속성 & 규칙
- `value` : String
  - null 불가
  - 최대 2000자
  - 빈 문자열("") 허용 (설명 없음을 표현)

### 금액 (Money)
_Value Object_

#### 속성 & 규칙
- `amount` : int
  - 음수 불가 (0 허용 - 무료 상품)

### 재고 수량 (StockQuantity)
_Value Object_

#### 속성 & 규칙
- `value` : int
  - 음수 불가 (0 허용 - 품절 상태 표현)

### 이미지 URL (ImageUrl)
_Value Object_

#### 속성 & 규칙
- `value` : String
  - null, 공백 불가
  - `http://` 또는 `https://` 로 시작해야 한다

---

## [ 주문 애그리거트 ]

### 주문 (Order)
_Aggregate Root_

#### 행위 & 규칙
- 주문 생성 : `static place(PlaceOrderCommand command, IdGenerator idGenerator)`
  - 생성 시 상태는 `PAYMENT_WAITING`
  - 주문 상품 목록은 1개 이상이어야 한다
  - 총 가격은 주문 상품 목록의 (단가 × 수량) 합산으로 자동 계산된다
  - 주문 시점의 배송지 정보(수신자, 주소)를 스냅샷으로 저장한다
  - 주문 시간(`orderedAt`) 기록
- 주문 취소 : `cancel()`
  - `PAYMENT_WAITING`, `PREPARING` 상태인 주문만 취소할 수 있다
  - 취소 시 상태는 `CANCELED`

#### 속성
- `id` : OrderId
- `memberId` : MemberId (회원 애그리거트 참조)
- `items` : List\<OrderItem\>
- `totalPrice` : Money (VO)
- `shippingInfo` : ShippingInfo (VO)
- `status` : OrderStatus
- `orderedAt` : LocalDateTime

### 주문 상태 (OrderStatus)
_Enum_
- `PAYMENT_WAITING` : 결제 대기
- `PREPARING` : 상품 준비 중
- `SHIPPED` : 배송 시작
- `DELIVERING` : 배송 중
- `DELIVERY_COMPLETED` : 배송 완료
- `CANCELED` : 취소 (PAYMENT_WAITING, PREPARING 상태에서만 전환 가능)

### 주문 상품 (OrderItem)
_Entity_

#### 속성 & 규칙
- `id` : OrderItemId
- `productId` : ProductId (상품 애그리거트 참조)
- `quantity` : int
  - 1개 이상이어야 한다
- `price` : Money (VO, 주문 시점의 단가 스냅샷)

### 배송 정보 (ShippingInfo)
_Value Object_

#### 속성 & 규칙
- `receiver` : Receiver (VO)
- `address` : Address (VO)
- 둘 다 null일 수 없다
- 주문 시점의 배송지를 스냅샷으로 보관하므로 이후 회원의 배송지 변경/삭제에 영향받지 않는다

---

## [ 장바구니 애그리거트 ]

### 장바구니 (Cart)
_Aggregate Root_

#### 행위 & 규칙
- 장바구니 생성 : `static create(MemberId memberId)`
  - `MemberId`가 장바구니의 식별자를 겸한다 — 회원 1명당 장바구니는 1개이므로 별도 식별자가 불필요하다
  - 생성 시 CartItems는 비어있다
- 상품 추가 : `addItem(AddCartItemCommand command, IdGenerator idGenerator)`
  - 이미 담긴 상품(`ProductId` 기준)이면 수량을 합산한다 — 새 CartItem을 생성하지 않는다
  - 수량은 1 이상이어야 한다
- 수량 변경 : `changeQuantity(CartItemId itemId, int quantity)`
  - 존재하지 않는 CartItemId로 변경 시 예외가 발생한다
  - 수량은 1 이상이어야 한다
- 항목 제거 : `removeItem(CartItemId itemId)`
  - 존재하지 않는 CartItemId로 제거 시 예외가 발생한다
- 선택 항목 제거 : `removeItems(List<CartItemId> itemIds)`
  - 부분 주문 완료 시 주문된 항목들만 제거할 때 사용한다
- 전체 비우기 : `clear()`
  - 전체 주문 완료 시 사용한다
  - 이미 비어있는 경우에도 예외 없이 정상 처리된다
- 담긴 상품 ID 목록 반환 : `getProductIds()`
  - Application Layer에서 상품 정보를 조회할 때 사용한다

#### 속성
- `memberId` : MemberId (식별자 겸 회원 애그리거트 참조)
- `items` : CartItems (일급 컬렉션)

### 장바구니 항목 (CartItem)
_Entity_

#### 속성 & 규칙
- `id` : CartItemId
- `productId` : ProductId (상품 애그리거트 참조)
- `quantity` : int
  - 1 이상이어야 한다
