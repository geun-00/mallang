spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        highlight_sql: true

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
logging:
  level:
    io.mallang.product.adapter.persistence.mybatis: trace
